# Local demo credentials (do not commit real secrets)
POSTGRES_USER=quant
POSTGRES_PASSWORD=change_me
POSTGRES_DB=quantdb

# Used by the backtester container
DATABASE_URL=postgresql+psycopg2://quant:change_me@postgres:5432/quantdb
